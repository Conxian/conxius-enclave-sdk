---
number: 235
title: "fix(wasm): close runtime evidence review gaps"
state: closed
labels: none
author: charliecreates[bot]
assignee: CharlieHelps
created: 2026-07-21
updated: 2026-07-21
merged: 2026-07-21
url: https://github.com/Conxian/conxius-enclave-sdk/pull/235
source: github
branch: fix/200-wasm-runtime-evidence-review-followup-v2 -> main
---

# fix(wasm): close runtime evidence review gaps

**PR #235** | CLOSED | Author: charliecreates[bot] | Created: 2026-07-21 | Merged: 2026-07-21

Branch: `fix/200-wasm-runtime-evidence-review-followup-v2` → `main`

## Summary

This is a narrowly scoped follow-up on current `main` (`dbd0316ce9b6c49da8445f4fd3bda8a11020d09c`) for the three refinements identified during review:

- Validate Lightning payment hash, BOLT11 invoice, and amount inputs at the WASM boundary with stable `INVALID_INPUT` errors that do not echo rejected caller values; cover both the direct constructor and factory path.
- Keep the active runtime workflow on the pinned Playwright package's managed Chromium executable, invoke that exact path, and record the Playwright version, browser version, executable path, and SHA-256 evidence.
- Check out the exact pull-request head SHA for PR workflows, record the merge-ref SHA separately, and fail closed when the tested checkout does not match the expected PR head.

The active `.github/workflows/wasm-runtime.yml` and `tests/wasm` harness remain the source of runtime evidence. Superseded follow-up workflow/script files were not restored.

## Review context

- Original PR: https://github.com/Conxian/conxius-enclave-sdk/pull/229
- Formal review: https://github.com/Conxian/conxius-enclave-sdk/pull/229#pullrequestreview-4742629537
- Repair source: `43987fa665744c210092bd69edfa2953224afbb6`
- Refs #200

## Verification

Verified locally at commit `46df346475673738ab96ea63717a98ca27f81104`:

- `cargo fmt --all -- --check` — passed.
- `cargo test --all-features` — 280 unit tests, 7 integration tests, and doc tests passed; 1 doc test remained ignored by its existing annotation.
- `cargo clippy --all-features -- -D warnings` — passed.
- Focused tests — `cargo test --all-features protocol::lightning::tests` (4 passed) and `cargo test --all-features wasm_support::tests` (8 passed).
- `bash -n scripts/wasm_runtime_evidence.sh`, `node --check tests/wasm/checks.mjs`, `node --check tests/wasm/run.mjs`, YAML parse, and `git diff --check` — passed.
- Active full harness: `CI=1 WASM_EXPECTED_TESTED_SHA=46df346475673738ab96ea63717a98ca27f81104 WASM_RUNTIME_EVIDENCE_DIR=/tmp/conxius-wasm-runtime-evidence-final ./scripts/wasm_runtime_evidence.sh` — Node, browser, Web Worker, and bundler lanes passed.
- Runtime evidence recorded tested head `46df346475673738ab96ea63717a98ca27f81104`, Playwright `1.54.2`, managed Chromium `139.0.7258.5`, executable SHA-256 `42327659f5f424d6410c2f488a5f1de6dabd1d329dc6ce6dee20a2a9ae48ad82` in the local ARM64 environment.
- Negative provenance check — a deliberate PR-head mismatch failed closed with `provenance_failure=pr-head-mismatch`.

## Support boundary

These changes improve deterministic negative/runtime/boundary evidence only. They do not establish an approved provider, hardware attestation, production support, release acceptance, or production readiness for WASM or Lightning.
