---
number: 34
title: "fix(enclave): validate simulated key bytes in place"
state: closed
labels: none
author: charliecreates[bot]
assignee: botshelomokoka
created: 2026-04-16
updated: 2026-04-29
merged: 2026-04-16
url: https://github.com/Conxian/conxius-enclave-sdk/pull/34
source: github
branch: fix/cloud-enclave-simulated-key-verify -> master
---

# fix(enclave): validate simulated key bytes in place

**PR #34** | CLOSED | Author: charliecreates[bot] | Created: 2026-04-16 | Merged: 2026-04-16

Branch: `fix/cloud-enclave-simulated-key-verify` → `master`

## Description
Follow-up to #29: validate the simulated KMS key bytes in place (inside the `Zeroizing<[u8; 32]>` buffer) to avoid copying the key bytes onto the stack just to run the secp256k1 validity check.

Fixes #

## Type of change
- [x] Bug fix (non-breaking change which fixes an issue)
- [ ] New feature (non-breaking change which adds functionality)
- [ ] Breaking change (fix or feature that would cause existing functionality to not work as expected)
- [ ] Documentation update
- [ ] Performance improvement

## Security & Integrity Checklist
- [x] **Zero Secret Egress**: I have verified that private keys and seeds never leave the secure hardware boundary.
- [x] **No Panics**: I have avoided `unwrap()`, `expect()`, and `panic!()`. All errors are handled via `ConclaveError`.
- [x] **Zeroization**: Sensitive material is zeroized after use using the `zeroize` crate.
- [ ] **Hardware Attestation**: If applicable, this change respects mandatory device integrity checks for high-value operations.
- [ ] **ISO 20022 Compliance**: If modifying financial messaging, I have verified the schema alignment.

## Verification Checklist
- [x] `cargo test` passes locally.
- [x] `cargo fmt --all -- --check` passes.
- [x] `cargo clippy -- -D warnings` passes.
- [ ] `wasm-pack build` passes (if changes affect the WASM surface).
- [ ] I have added tests that prove my fix is effective or that my feature works.

## Verification
```bash
cargo fmt --all -- --check
cargo clippy -- -D warnings
cargo test
```

reviewChanges skipped: `src/enclave/cloud.rs:62-74` (unsafe FFI) — intentional to validate the `Zeroizing<[u8; 32]>` buffer in place; the safe `SecretKey::from_byte_array` API requires passing an owned `[u8; 32]` which copies key material out of the zeroizing buffer.
