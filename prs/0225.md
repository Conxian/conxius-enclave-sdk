---
number: 225
title: "fix: quarantine residual Ark and BitVM2 protocol bypasses"
state: closed
labels: none
author: charliecreates[bot]
assignee: unassigned
created: 2026-07-21
updated: 2026-07-21
merged: 2026-07-21
url: https://github.com/Conxian/conxius-enclave-sdk/pull/225
source: github
branch: charlie/issue-197-quarantine-hardening -> main
---

# fix: quarantine residual Ark and BitVM2 protocol bypasses

**PR #225** | CLOSED | Author: charliecreates[bot] | Created: 2026-07-21 | Merged: 2026-07-21

Branch: `charlie/issue-197-quarantine-hardening` → `main`

## Summary
- Fail closed for `ArkBackend::ProviderOwned`.
- Quarantine legacy WASM BitVM signing and aggregation before decoding or native calls.
- Return stable typed/WASM errors; documentation and capability evidence remain explicitly unsupported.

## Why
Completes residual containment after PR #219 without claiming protocol conformance.

## Breaking change / migration
`ArkManager::with_backend` now returns `ConclaveResult<Self>`. Callers must handle the result and propagate or handle the error.

## Verification
- Mandatory native gate passed: `cargo fmt --all -- --check && cargo clippy --all-features -- -D warnings && cargo test` (232 unit tests, 5 integration tests, and passing doc-test coverage with one expected ignored test).
- Focused Ark, BitVM2, WASM quarantine regressions passed.
- Capability-evidence validation passed: `python3 scripts/validate_capability_evidence.py --check`.
- Production containment integration tests passed: `cargo test --test production_rail_containment`.
- Attempted `cargo check --target wasm32-unknown-unknown --all-features`; it remains blocked by the existing `secp256k1-sys`/Clang `memmove` compile failure on this target.
- Generated WASM runtime tests were not executed due existing target/dependency limitations.

## Scope / remaining work
- #195/#196 and audited implementations, vectors, provider evidence, and release evidence remain open.
- Production support remains unavailable; this change is containment/quarantine, not protocol implementation or conformance.

Refs #197
