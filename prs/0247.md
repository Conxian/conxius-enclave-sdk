---
number: 247
title: "feat(enclave): add trust and durable replay foundations"
state: closed
labels: none
author: charliecreates[bot]
assignee: unassigned
created: 2026-07-22
updated: 2026-07-22
merged: 2026-07-22
url: https://github.com/Conxian/conxius-enclave-sdk/pull/247
source: github
branch: charlie/issue-240-trust-replay-foundation -> main
---

# feat(enclave): add trust and durable replay foundations

**PR #247** | CLOSED | Author: charliecreates[bot] | Created: 2026-07-22 | Merged: 2026-07-22

Branch: `charlie/issue-240-trust-replay-foundation` → `main`

## Summary
- Add provider-neutral, versioned authenticated trust bundles and receipts with fail-closed validation.
- Complete domain-separated replay binding and the typed `ReplayStore` consume-once/batch contract.
- Enforce durable-only proof authorization, final signing, and `RailProxy` gates; local paths fail closed.

## Security posture
This is a contract/foundation PR only. The repository remains Beta/conditional. Production provider routes remain unavailable. This PR claims no real distributed backend, provider roots, collateral, revocation runtime, or provider deployment evidence.

## Verification
Verified at `46dcc99361d76f31a812638454d863834fd6ef0d`:
- `cargo fmt --all -- --check` — passed.
- `python3 scripts/validate_capability_evidence.py --check` — passed (70 capabilities).
- `cargo clippy --all-features -- -D warnings` — passed.
- `cargo test` — passed (374 unit + 6 rail + 6 proof + docs).
- `cargo test --all-targets --all-features` — passed (392 unit + 7 rail + 6 proof + docs).
- `cargo check --release --no-default-features` — passed.
- Focused trust, replay, proofs, and RailProxy test suites — passed.
- `git diff --check` and `git diff --cached --check` — passed.
- Pre-PR security audit — no blocker, high, or medium findings.

## Remaining work
- Implement a real durable backend and restart/replica/region failure tests.
- Add provider verifiers, roots, collateral, revocation, TCB, and measurement evidence.
- Produce trusted-clock and hardware-runtime evidence.
- Complete independent review and exact release artifacts, SBOM, and provenance.

Refs #240
Refs #195
Refs #241
Refs #242

The four foundation commits are:
- `feat(enclave): add trust and replay foundations`
- `fix(enclave): harden trust and replay boundaries`
- `fix(enclave): require durable value replay`
- `fix(rails): require durable replay boundaries`
