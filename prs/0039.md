---
number: 39
title: "refactor(settlement): dedupe namespace scope parsing"
state: closed
labels: none
author: charliecreates[bot]
assignee: botshelomokoka
created: 2026-04-16
updated: 2026-04-18
merged: 2026-04-18
url: https://github.com/Conxian/conxius-enclave-sdk/pull/39
source: github
branch: charlie/refactor-iso20022-namespace-scope -> master
---

# refactor(settlement): dedupe namespace scope parsing

**PR #39** | CLOSED | Author: charliecreates[bot] | Created: 2026-04-16 | Merged: 2026-04-18

Branch: `charlie/refactor-iso20022-namespace-scope` → `master`

Follow-up to the PR #36 review: reduce drift risk in `validate_iso20022_trigger_payload` by consolidating the `xmlns` / `xmlns:` parsing used by both `Event::Start` and `Event::Empty`.

## Description
- Extracted `xmlns` parsing into `build_namespace_scope(...)` so both element paths share the same logic.
- Kept behavior the same (still rejects malformed XML / invalid UTF-8 in namespace values) while shrinking the duplicated surface.

Fixes #

## Type of change
- [x] Bug fix (non-breaking change which fixes an issue)
- [ ] New feature (non-breaking change which adds functionality)
- [ ] Breaking change (fix or feature that would cause existing functionality to not work as expected)
- [ ] Documentation update
- [ ] Performance improvement

## Security & Integrity Checklist
- [ ] **Zero Secret Egress**: I have verified that private keys and seeds never leave the secure hardware boundary.
- [x] **No Panics**: I have avoided `unwrap()`, `expect()`, and `panic!()`. All errors are handled via `ConclaveError`.
- [ ] **Zeroization**: Sensitive material is zeroized after use using the `zeroize` crate.
- [ ] **Hardware Attestation**: If applicable, this change respects mandatory device integrity checks for high-value operations.
- [x] **ISO 20022 Compliance**: If modifying financial messaging, I have verified the schema alignment.

## Verification Checklist
- [x] `cargo test` passes locally.
- [x] `cargo fmt --all -- --check` passes.
- [x] `cargo clippy -- -D warnings` passes.
- [ ] `wasm-pack build` passes (if changes affect the WASM surface).
- [ ] I have added tests that prove my fix is effective or that my feature works.

## Verification
```bash
# rustfmt: pass
cargo fmt --all -- --check

# clippy: pass
cargo clippy -- -D warnings

# tests: 9 passed
cargo test

# wasm-pack: fails locally (preexisting) with `secp256k1-sys` `memmove` error
wasm-pack build
```

reviewChanges skipped: suggestion to switch namespace checks to raw byte prefix matching — it would change current behavior by potentially accepting non-UTF-8 namespace values.
