---
number: 234
title: "Restore fail-closed controls after PR #232"
state: closed
labels: none
author: charliecreates[bot]
assignee: unassigned
created: 2026-07-21
updated: 2026-07-22
merged: 2026-07-22
url: https://github.com/Conxian/conxius-enclave-sdk/pull/234
source: github
branch: charlie/fail-closed-pr-232-remediation -> main
---

# Restore fail-closed controls after PR #232

**PR #234** | CLOSED | Author: charliecreates[bot] | Created: 2026-07-21 | Merged: 2026-07-22

Branch: `charlie/fail-closed-pr-232-remediation` → `main`

## Summary

PR #232 was squash-merged even though its formal security review requested changes. This follow-up restores the pre-#232 fail-closed baseline rather than exposing unsupported positive hardware verification.

- Restores release secret-scan, SBOM/provenance, registry-verification, and release dependency gates.
- Restores the WASM runtime-evidence workflow, harnesses, browser fixture, and fail-closed WASM/Ark boundaries.
- Restores `ProviderVerifier::Unavailable` for production policy; no positive `VerifiedHardware` or production-positive X.509 path is claimed.
- Preserves the genuine dynamic test-nonce hardening from commits `2072b402467a6e9d9ea610ece7c5b51680451a4d` and `0499bad40f3537b30321dff335dc27e8994228b9`.
- Real provider roots and collateral remain required before positive hardware support can be enabled.

Formal review: https://github.com/Conxian/conxius-enclave-sdk/pull/232#pullrequestreview-4743788066

Potential overlap/conflict with open PR #231 remains; this PR does not claim to supersede it.

Refs #232

## Verification

Passed:

- `git diff --check`
- `cargo fmt --all -- --check`
- `cargo clippy --all-features -- -D warnings`
- `cargo test` — 243 unit tests, 6 integration tests, and doc tests passed
- `python3 scripts/validate_capability_evidence.py --check`
- YAML parsing for all `.github/workflows/*.yml`
- `node --check` for all four JavaScript runtime files
- `cargo check --target wasm32-unknown-unknown --locked` with the repository CFLAGS workaround for the devbox clang/dependency `memmove` header issue

The repository WASM runtime evidence command was attempted but blocked before build by the environment because `wasm-pack` is not installed. No runtime artifacts are claimed.
