---
number: 29
title: "fix(enclave): safe dev keys and unlock zeroization"
state: closed
labels: none
author: charliecreates[bot]
assignee: botshelomokoka
created: 2026-04-16
updated: 2026-04-29
merged: 2026-04-16
url: https://github.com/Conxian/conxius-enclave-sdk/pull/29
source: github
branch: fix/cloud-enclave-dev-key-zeroize -> master
---

# fix(enclave): safe dev keys and unlock zeroization

**PR #29** | CLOSED | Author: charliecreates[bot] | Created: 2026-04-16 | Merged: 2026-04-16

Branch: `fix/cloud-enclave-dev-key-zeroize` → `master`

Follow-up fixes for the PR #27 review thread.

### Changes
- `CoreEnclaveManager::unlock`: derive PBKDF2 output directly into `Zeroizing<[u8; 64]>` to avoid leaving an extra non-zeroized stack copy.
- `CloudEnclave`: make `with_dev_key()` fallible, store the dev key in `Zeroizing`, and remove the fixed `[0xCD; 32]` fallback by generating a per-instance ephemeral key.

### Verification
```bash
# tests: 8 passed
$ cargo test

# rustfmt: pass
$ cargo fmt --all -- --check

# clippy: pass
$ cargo clippy -- -D warnings

# wasm-pack: fails locally (also fails on origin/master)
$ wasm-pack build
```

- Note: `wasm-pack build` currently fails for `wasm32-unknown-unknown` due to `secp256k1-sys`'s bundled wasm sysroot missing a `memmove` declaration (reproducible on `origin/master` as well).
- reviewChanges skipped: `src/protocol/job_card.rs:40-55` amount validation comment is out of scope for this enclave-only PR.
