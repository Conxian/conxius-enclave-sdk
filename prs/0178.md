---
number: 178
title: "chore: bump version to 2.0.12"
state: closed
labels: none
author: charliecreates[bot]
assignee: unassigned
created: 2026-07-15
updated: 2026-07-15
merged: 2026-07-15
url: https://github.com/Conxian/conxius-enclave-sdk/pull/178
source: github
branch: fix/issue-176-clippy-release-2.0.12 -> main
---

# chore: bump version to 2.0.12

**PR #178** | CLOSED | Author: charliecreates[bot] | Created: 2026-07-15 | Merged: 2026-07-15

Branch: `fix/issue-176-clippy-release-2.0.12` → `main`

## Summary

- Fix the stable-Clippy `only_used_in_recursion` error in `BitVm2Orchestrator::calculate_tree_root` by making it an associated helper without `&self`; hashing and error behavior are unchanged.
- Bump the crate version from `2.0.11` to `2.0.12` and add the release-facing changelog entry.
- Update active current-version metadata in `README.md` and `TRACKING.md` to `v2.0.12`.
- Keep `Cargo.lock` ignored and untracked; no `Cargo.lock` change is included in this PR.
- Keep `rust-version = "1.85"` unchanged; the required verification ran with Rust 1.91.0.

## Root cause and prerequisite fix

Release validation could not complete because `2.0.11` is already published on crates.io. Before the version bump, stable Clippy also failed on the existing recursive helper because its receiver was only used for recursion. The minimal associated-function refactor restores a warning-free baseline without suppressing the lint or changing behavior.

## Verification

All Rust commands below used `RUSTUP_TOOLCHAIN=1.91.0`:

- `cargo fmt --all -- --check`
- `cargo clippy -- -D warnings`
- `cargo test` — 124 passed, 0 failed; 1 doctest ignored
- `.github/scripts/verify-release-metadata.sh 2.0.12`
- `cargo metadata --no-deps --format-version 1` — `conxius-enclave-sdk 2.0.12`
- `cargo package --allow-dirty` — packaged and verified `conxius-enclave-sdk v2.0.12`

The final worktree is clean, `Cargo.lock` is absent and still covered by `.gitignore`, and the PR diff is limited to four files with no lockfile. No publish or release workflow was dispatched.

Resolves #176

