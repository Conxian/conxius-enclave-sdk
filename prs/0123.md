---
number: 123
title: "Remediate CI checks and harden workflows"
state: closed
labels: none
author: botshelomokoka
assignee: unassigned
created: 2026-06-20
updated: 2026-06-20
merged: 2026-06-20
url: https://github.com/Conxian/conxius-enclave-sdk/pull/123
source: github
branch: remediate-ci-checks-12704398788998405357 -> main
---

# Remediate CI checks and harden workflows

**PR #123** | CLOSED | Author: botshelomokoka | Created: 2026-06-20 | Merged: 2026-06-20

Branch: `remediate-ci-checks-12704398788998405357` → `main`

Remediated CI failures and hardened repository workflows. 

Key improvements:
1. **GitHub Actions Hardening**: 
   - Standardized `actions/checkout` version comments to `# v4.2.2` to match the pinned SHA.
   - Replaced insecure `curl | sh` pipe for `wasm-pack` installation in `ci.yml` with `taiki-e/install-action` pinned to a specific SHA.
   - Corrected `codeql.yml` to use proper `autobuild` and `analyze` steps.

2. **WASM Binding Remediations**:
   - Fixed compilation errors in `src/wasm_bindings.rs` caused by attempting to return types with `Arc<dyn EnclaveManager>` or lifetimes to JS. Introduced `WasmBitcoinManager`, `WasmEthereumManager`, and `WasmSolanaManager` wrappers.
   - Added `Chain::COSMOS` to `register_asset` to align with the `AssetRegistry`.
   - Implemented `From<ConclaveError> for JsValue` in `src/lib.rs` for standardized error handling in WASM.
   - Added `#[wasm_bindgen(getter_with_clone)]` to `SdkConfig` fields to satisfy WASM visibility requirements.

3. **Verification**:
   - `cargo test`: 54/54 passed.
   - `cargo check --target wasm32-unknown-unknown`: Passed (verified with required CFLAGS).
   - `cargo fmt`: All files formatted correctly.

---
*PR created automatically by Jules for task [12704398788998405357](https://jules.google.com/task/12704398788998405357) started by @admin-conxian-labs*
