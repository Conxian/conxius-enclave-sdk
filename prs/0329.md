---
number: 329
title: "docs(kb): session 64 audit remediation + live verification + crates.io cleanup"
state: closed
labels: none
author: botshelomokoka
assignee: unassigned
created: 2026-08-31
updated: 2026-08-31
merged: 2026-08-31
url: https://github.com/Conxian/conxius-enclave-sdk/pull/329
source: github
branch: docs/kb-audit-session64 -> main
---

# docs(kb): session 64 audit remediation + live verification + crates.io cleanup

**PR #329** | CLOSED | Author: botshelomokoka | Created: 2026-08-31 | Merged: 2026-08-31

Branch: `docs/kb-audit-session64` → `main`

## Summary
Session 64 full KB → code → CI → cross-repo audit remediation, plus live toolchain verification and crates.io account cleanup.

## KB / code alignment (documentation drift fixed)
- Cross-repo PR state corrected: #250/#255/#280/#281 merged; #251 open; #327/#328 merged.
- Protocol module count 49 → 50 (`fedimint_crypto`); `src/re_exports.rs` → `src/lib.rs`; `SystemState::initialize()` → `EnclaveManager::initialize()`.
- Version drift: `v2.0.16` → `v2.0.17` (README / RELEASING / PRODUCTION_READINESS / REPOSITORY_ANALYSIS); chain count `30+` → `42`; MSRV `1.94.1` → `1.97.1`.

## Live verification (first full toolchain run)
Installed Rust 1.97.1 + clippy/rustfmt + system deps and ran the CI gate against the committed `Cargo.lock`:
- `cargo test --locked` → 629 passed / 0 failed (confirms the AGENTS.md "629 tests" figure exactly)
- `cargo test --locked --all-features` → 645 passed / 0 failed / 2 ignored
- `cargo fmt --all -- --check` → clean
- `cargo clippy --locked --all-targets --all-features -- -D warnings` → clean (0 warnings)

## Dependency security scan
- `cargo audit` → 0 vulnerabilities (clean after adding `atomic-polyfill` / `RUSTSEC-2023-0089` to `.cargo/audit.toml`)
- `cargo deny check` → advisories ok, bans ok, licenses ok, sources ok

## crates.io account cleanup (performed, not just documented)
- Yanked `lib-conclave-sdk@2.0.8` (stale pre-rebrand name) — DEP-003 resolved.
- Yanked `anya-core@1.2.0` (deprecated `anya-org` asset absorbed into Conxian).

## Config drift fixes
- `.cargo/audit.toml`: added `RUSTSEC-2023-0089` (atomic-polyfill) to match `deny.toml`.
- Removed orphaned root `audit.toml` (cargo-audit reads `.cargo/audit.toml`, not the root file).
- Reconciled `DEBT_INVENTORY.md` DEP-002 advisory list against the live 562-dep graph.

## Notes
- `AGENTS.md` is modified; per CODEOWNERS this may require `admin-conxian-labs` approval.
- This PR was created by an AI agent (OpenHands) on behalf of Botshelo Mokoka.

@botshelomokoka can click here to [continue refining the PR](https://app.all-hands.dev/conversations/c5073a24-4a51-44a4-9627-5e66f7b796a9)
