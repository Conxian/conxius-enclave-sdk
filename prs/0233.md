---
number: 233
title: "fix: canonicalize Bitcoin and Ethereum verification"
state: closed
labels: bitcoin, cryptography, priority-critical
author: charliecreates[bot]
assignee: unassigned
created: 2026-07-21
updated: 2026-07-21
merged: 2026-07-21
url: https://github.com/Conxian/conxius-enclave-sdk/pull/233
source: github
branch: charlie/issue-196-canonical-verification -> main
---

# fix: canonicalize Bitcoin and Ethereum verification

**PR #233** | CLOSED | Author: charliecreates[bot] | Created: 2026-07-21 | Merged: 2026-07-21

Branch: `charlie/issue-196-canonical-verification` → `main`

## Summary

- Canonical BIP-341 odd-Y Taproot secret normalization and strict BIP-340 verification with official vectors.
- Canonical Ethereum EIP-55/EIP-191 signature/address validation, low-S and recovery binding, explicit EIP-2098, and checked EIP-155 `v` decoding.
- Typed, network-aware BIP-322 verification for supported native Simple P2WPKH and P2TR key-path forms, with official vectors and fail-closed boundaries.
- Scoped capability/evidence reconciliation.

## Supported boundary

- Native BIP-322 P2WPKH and P2TR key-path are cryptographically verified.
- Structurally checked unsupported Simple P2WSH/Taproot script paths remain `Inconclusive`.
- Full and Proof-of-Funds decode failures are errors; decoded forms return dedicated typed unsupported-format errors.
- Full script interpretation, EIP-1559/EIP-712 transaction construction, provider/hardware evidence, independent review, release artifacts, and production support remain out of scope/unsupported.
- #195 remains the production authorization/provider gate.

## Verification

- `cargo fmt --all -- --check` — passed.
- `cargo clippy --locked --all-targets --all-features -- -D warnings` — passed.
- `cargo test --locked --all-features` — passed: 268 unit tests, 7 integration tests, and doc tests with 1 passed / 1 ignored.
- `cargo test --locked --all-features --all-targets` — passed: 268 unit tests, 7 integration tests, and all example targets compiled and ran with zero tests.
- No-default and feature matrix checks/tests passed for `bip110_compliant`, `development-simulators`, and `mock-cloud-enclave`.
- Exact CI WASM build passed: `CARGO_HTTP_MULTIPLEXING=false CFLAGS="-Dmemmove=__builtin_memmove -Dmemcpy=__builtin_memcpy -Dmemset=__builtin_memset -Dmemcmp=__builtin_memcmp" wasm-pack build --release --target bundler -- --locked`.
- Capability evidence validation passed: `54 capabilities; generated matrix is current`; Python validation suite passed with 7 tests.
- `cargo audit --file Cargo.lock` passed; `cargo deny --config deny.toml --locked check advisories bans licenses sources` passed with existing dependency-duplication and allow-list warnings.
- `git diff --check`, `git diff --cached --check`, and `git merge-tree --write-tree origin/main HEAD` passed with a clean merge tree.
- Focused evidence snapshot: `docs/audits/ISSUE-196_CANONICAL_VERIFICATION_EVIDENCE_2026-07-21.md`.

## Risk / review focus

- Odd-Y Taproot normalization and provider semantics.
- BIP-322 `Valid`/`Invalid`/`Inconclusive`/`Unsupported` boundaries and unprefixed Simple compatibility.
- Ethereum 64-byte `r || s` versus explicit EIP-2098 semantics and recovery parity.

Resolves #196
Refs #195
Refs #202
