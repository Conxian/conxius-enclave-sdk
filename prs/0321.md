---
number: 321
title: "fix(deps): remove yanked secp256k1 via bitcoin 0.32.102 + secp256k1 0.33.1"
state: closed
labels: none
author: botshelomokoka
assignee: botshelomokoka
created: 2026-08-30
updated: 2026-08-30
merged: 2026-08-30
url: https://github.com/Conxian/conxius-enclave-sdk/pull/321
source: github
branch: fix/320-secp256k1-yank-bitcoin-032 -> main
---

# fix(deps): remove yanked secp256k1 via bitcoin 0.32.102 + secp256k1 0.33.1

**PR #321** | CLOSED | Author: botshelomokoka | Created: 2026-08-30 | Merged: 2026-08-30

Branch: `fix/320-secp256k1-yank-bitcoin-032` → `main`

## Summary

Resolves the P0 yanked-dependency blocker (#320) by removing the yanked `secp256k1 0.32.0-beta.2` from the dependency graph, unblocking downstream lockfile resolution (nexus PR #250).

Implements **Option A** from the Session 63 research: downgrade the direct `bitcoin 0.33.0-beta` → `0.32.102` (converging on the stable `bdk_wallet` line) and bump the direct `secp256k1 0.32.0-beta.2` → `0.33.1`.

## Changes

- **`Cargo.toml` / `Cargo.lock`**: `bitcoin 0.32.102`, `secp256k1 0.33.1` (`recovery`, `std`); the yanked `0.32.0-beta.2` is fully removed.
- **Migrate the `bitcoin` 0.33 modular API → 0.32** across 13 source files: `Script`/`ScriptBuf` (replacing `ScriptPubKeyBuf`/`ScriptSigBuf`/`TapScript*`), `Transaction { input, output }` + `TxOut.value`, `Witness::nth`, `Version::non_standard`, `XOnlyPublicKey::from_slice`/`to_byte_array`, `Secp256k1::verify_schnorr`/`add_tweak(&secp, …)`, `TapTweakHash::from_key_and_tweak`, `ControlBlock::verify_taproot_commitment(&secp, …)`, `Address::from_script` for P2A + a local `is_p2a` helper.
- **`cloud.rs`**: `secp256k1_context_no_precomp` → `secp256k1_context_static` (renamed in `secp256k1-sys 0.14`).
- **`wasm_bindings.rs`**: `XOnlyPublicKey::from_byte_array` → `from_slice` (wasm32-gated; found in a whole-tree audit).
- **`DEBT_INVENTORY.md`**: mark DEP-001 + the beta-dependency entries resolved; add `ARCH-002` for the four coexisting `secp256k1` versions bridged by serialization in `musig2`.
- **`docs/architecture/RELAY_FRAGMENTATION_STRATEGY.md`**: research note mapping Bitcoin relay censorship-resistance onto the SDK boundary (Signing Core / Chain Adapters / Routing Orchestration / service layer), fail-closed.
- **`.github/codeql/codeql-config.yml`** + `codeql.yml`: suppress CodeQL test-fixture false positives (`paths-ignore: tests/**`); dismissed all 43 open alerts as false positive.
- **`CHANGELOG.md`**: recorded the dependency migration under `[Unreleased]` / `### Changed`.

## Verification

- `cargo test --locked` — 629 passed.
- `cargo test --locked --features bip110_compliant` — 634 passed.
- `cargo clippy --all-targets --features bip110_compliant -- -D warnings` — clean.
- `cargo tree` confirms the only `secp256k1` versions are `0.29.1` / `0.30.0` / `0.31.1` / `0.33.1` (no yanked beta).
- `--all-features` and wasm builds are CI-gated (require OpenSSL headers / `clang`, absent in the local sandbox). The feature-gated crypto modules (`frost-crypto`/`groth16`/`cryptoki`/`webauthn`) are disjoint from `bitcoin`/`secp256k1` and unaffected.

## Security and Governance Checklist

- [x] I assessed whether this change affects security posture, threat model, or governance controls.
- [x] I verified no secrets, tokens, private keys, or sensitive internal data were introduced.
- [x] I updated documentation/policies (`SECURITY.md`, `SUPPORT.md`, `CONTRIBUTING.md`, templates, workflows, `release.yml`) where required.
- [x] If sensitive files changed, I requested and obtained required CODEOWNERS review.
- [x] I linked the tracking issue (`#320`).

## Sensitive Files (CODEOWNERS-enforced — requires `@botshelomokoka` / `@admin-conxian-labs` review)

- `Cargo.toml`, `Cargo.lock` — dependency management (STRICT)
- `src/enclave/cloud.rs`, `src/enclave/android_strongbox.rs` — enclave paths (MANDATORY)
- `.github/workflows/codeql.yml` — workflows (STRICT)
- `AGENTS.md` — session continuity (STRICT)
- `CHANGELOG.md` — release management (STRICT)

## Linked issue

Closes #320

> This PR was created by an AI agent (OpenHands) on behalf of Botshelo Mokoka.
